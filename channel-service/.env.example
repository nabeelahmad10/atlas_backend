# Channel Service Environment Configuration
# Copy this file to .env and update the values

# Core App Settings
APP_ENV=development

# Webhook Callback URL (Where events are dispatched back to the main CRM)
CRM_CALLBACK_URL=http://localhost:8000/api/receipts
